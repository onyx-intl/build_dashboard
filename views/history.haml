%h1 Build history
%dl
  %dt Branch:
  %dd= @branch
  %dt Kernel:
  %dd= @kernel
  %dt Profile:
  %dd= @profile
%ul
  - @builds.each do |build|
    %li
      %a{:href => job_link(build[:num])}= build[:id]
