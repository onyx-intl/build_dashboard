%h1 Build artifacts
%h2= @build_id
%a{:href => "/"} Return to dashboard.
%ul
  - @artifacts.each do |artifact|
    %li
      = artifact
      %br/
      [
      %a{:href => "/upload/#{@build_id}/#{artifact}"} Make public
      ]
